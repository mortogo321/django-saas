from django.urls import path

# from .views import crate_checkout_session, order_cancel, order_success, stripe_webhook

urlpatterns = [
    # path(
    #     "<int:order_id>/",
    #     crate_checkout_session,
    #     name="create_checkout_session",
    # ),
    # path(
    #     "order_success/",
    #     order_success,
    #     name="order_success",
    # ),
    # path(
    #     "order_cancel/",
    #     order_cancel,
    #     name="order_cancel",
    # ),
    # path(
    #     "strip/webhook/",
    #     stripe_webhook,
    #     name="stripe_webhook",
    # ),
]
